service:
  api:
    port: "8090"
    enable: true

database:
  mongodb:
    uri: mongodb://root:root@localhost:27018/?authSource=admin
    databases: 
      blog: "blog"
      log: "log"
    collections:
      posts: "posts"
      logs: "logs"

broker:
  rabbitmq:
    uri: amqp://root:root@localhost:5672/
    queues: 
      hello: "hello"
