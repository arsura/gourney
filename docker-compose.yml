version: "3.8"

services:
  moonbase-service:
    build: .
    container_name: moonbase-service
    image: moonbase-service
    restart: unless-stopped
    ports:
      - 8080:8080

networks:
  default:
    external:
      name: moonbase-network
